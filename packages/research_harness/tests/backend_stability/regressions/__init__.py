"""Regression tests: one test case per historical P0/P1 backend-stability issue.

Each test docstring cites the feedback entry (date + title) so future
readers can trace context. When a test is added, the source entry in
new public issues should get a `Regression: test_xxx` annotation in the issue/PR.
"""
