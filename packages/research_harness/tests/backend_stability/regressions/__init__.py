"""Regression tests: one test case per historical P0/P1 from docs/feedback/.

Each test docstring cites the feedback entry (date + title) so future
readers can trace context. When a test is added, the source entry in
docs/feedback/bugs.md should get a `Regression: test_xxx` annotation.
"""
